from tkinter import *

root=Tk()
label1 = Label(root, text="Lukes window")
label2 = Label(root, text="Using the grid system")

label1.grid(row=0, column=0)
label2.grid(row=1, column=0)

root.mainloop()
