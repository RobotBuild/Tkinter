from tkinter import *

root = Tk()

my_canvas = Canvas(root, width=300, height=300, bg="white")
my_canvas.pack()

# This is just like the line widget, except it makes a rectangle. Point A, point B, then connect the dots.
my_canvas.create_rectangle(50, 50, 150, 150, fill="red")

root.mainloop()
